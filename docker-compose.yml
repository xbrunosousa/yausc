version: '3'

networks:
  web:
    external: true
  internal:
    external: false

services:
  nginx:
    image: bruno/yausc
    restart: always
    networks:
      - internal
      - web
    labels:
      - traefik.port=20101
      - traefik.docker.network=web
      - traefik.frontend.rule=Host:yausc.xyz
      - 'traefik.enable=true'
